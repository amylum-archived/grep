name 'grep'
org 'amylum'

licenses 'COPYING'

configure_flags(
  prefix: '/usr'
)

cflags(['-Werror=unused-macros'])
harden

build do
  run './bootstrap'
  configure
  make
  install
end

test do
  run 'grep --version'
end
