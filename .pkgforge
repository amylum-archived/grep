name 'grep'
org 'amylum'

licenses 'COPYING'

configure_flags(
  prefix: '/usr'
)

harden
cflags ["-Wnoerror=unused-macros"]

build do
  run './bootstrap'
  configure
  make
  install
end

test do
  run 'grep --version'
end
